# https://www.robotstxt.org/robotstxt.html
#
# The wildcard group below applies to ALL crawlers (including AI bots:
# GPTBot, ChatGPT-User, ClaudeBot, anthropic-ai, PerplexityBot, CCBot,
# Google-Extended). Per RFC 9309, a robots.txt parser selects the single
# most-specific matching User-agent group; rules are NOT inherited from
# wildcards. So we deliberately do NOT add per-AI-bot groups — that would
# silently exempt those bots from the Disallow rules below.

User-agent: *
Disallow: /account
Disallow: /invite
Disallow: /callback
Disallow: /u/
# Synthetic SPA-shell route. Used internally by TanStack Start to
# generate _shell.html for nginx's SPA fallback. Not a real page.
Disallow: /tss-spa-shell
# Note: /status is intentionally indexable. It's a public service-health
# page with no auth requirement. The body is live data that populates on
# the client; the prerendered <head> carries the SEO meta.

Sitemap: https://myscrollr.com/sitemap.xml
